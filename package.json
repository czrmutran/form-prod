{
  "name": "aurora-signup",
  "version": "1.0.0",
  "description": "Aurora sign-up flow (Amazon-style steps, original UI)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
